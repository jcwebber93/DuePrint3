if !exists(global.purgecenterx)								
	global purgecenterx = 127
if !exists(global.purgecentery)								
	global purgecentery = 178.5
if !exists(global.preprint_macro)								
	global preprint_macro = false
if !exists(global.home_zmax)								
	global home_zmax = true
if !exists(global.deploy_probe)								
	global deploy_probe = true
;if !exists(global.minx_special)								
;	global minx_special = xx
;if !exists(global.maxx_special)								
;	global maxx_special = xx
;if !exists(global.minz_special)								
;	global minz_special = xx


if !exists(global.support_toggle_pos)								
	global support_toggle_pos = -164.2
if !exists(global.model_toggle_pos)								
	global model_toggle_pos = 185.2
if !exists(global.probe_deploy_pos)								
	global probe_deploy_pos = -144.384
if !exists(global.probe_retract_pos)								
	global probe_retract_pos = 165.367
if !exists(global.probe_deploy_y)								
	global probe_deploy_y = 178.5